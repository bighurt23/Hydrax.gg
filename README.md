# Hydrax.GG

Free crypto-mining profit, ROI, and electricity calculators for GPU rigs and ASIC miners —
the front porch of the **HydraX** mining stack (the HydraX miner + Operator dashboard + Hydra chain).

Static site (HTML/CSS/JS, no build step). Deployed via **GitHub Pages** → `hydrax.gg`.

## Files
- `index.html` — page + copy + SEO
- `styles.css` — neon Hydra theme
- `app.js` — live prices (CoinGecko) + the 3 calculators
- `CNAME` — custom domain for GitHub Pages

## Editing
Just edit the files and push to `main` — GitHub Pages redeploys automatically.

## TODO (drop-in)
- `app.js` → `LINKS.buy` (Gumroad), `LINKS.operator` (hydraxoperator.com)
- 3 affiliate gear links (`data-aff` cards in `index.html`)
